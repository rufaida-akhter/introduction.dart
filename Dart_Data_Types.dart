void main()
{
  // Dart is static typed and types annotations language. Dart can infer a type and types annotations are optional.
// int marks = 80;
int marks = 1;
double pi=3.14;
double mymark=(marks+marks)*20+pi;
print("the mark is $marks+$mymark and $pi");
}